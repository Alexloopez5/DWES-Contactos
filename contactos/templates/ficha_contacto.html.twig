{% extends "base.html.twig" %}
{% block title %}Contactos{% endblock %}
{% block body %}
    <h1>Ficha del contacto</h1>
    {% if contacto %}
        {{ include ("partials/datos_contacto.html.twig", {'contacto': contacto}) }}
        <a id="bt" href="{{ path('editar_contacto', {'codigo': codigo}) }}" class="btn">Editar</a>
        <a id="bt" href="{{ path('eliminar_contacto', {'id': codigo}) }}" class="btn">Borrar</a>
    {% else %}
    <h4>Contacto no encontrado</h4>
    {% endif %}
{% endblock %}